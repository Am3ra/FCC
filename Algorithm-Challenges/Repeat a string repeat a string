function repeatStringNumTimes(str, num) {
  var newStr = ""; 
  if(num<=0){
   } else {
     for (i=1;i<=num;i++){
       newStr += str;
       console.log(newStr);
     }
   }
  return newStr;
}

console.log(repeatStringNumTimes("abc", 3));

